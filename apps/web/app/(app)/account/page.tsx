// 본인 계정 라우트 (AXKG-SPEC-008, WP6 FE-7) — staff·admin 공통(본인 자율 비밀번호 변경).
import { ChangePassword } from "@/components/account/change-password";

export default function AccountPage() {
  return <ChangePassword />;
}
