# CLAUDE.md — AI 스테이지 작업 workspace

이 디렉토리는 open-kknaks worker가 claude를 실행하는 **작업 프로젝트**다.
(ax-graph 레포 루트의 개발용 `CLAUDE.md`와 별개 — 이건 AI 스테이지 실행 전용이다.)
요약①·분류②·문서화③·채팅④ 스테이지를 모두 이 workspace에서 돌린다.

작업을 시작하기 전에 먼저 `agent.md`를 읽는다. `agent.md`가 이 workspace의 진입점이다.
